// TODO: extend Component and implement render() method for pill icon 
class Pill {
    pillName;
    pillSize;
    pillColour;
    pillInformation;
    pillRepeats = [0, 0, 0, 0, 0, 0, 0];
    pillFrequency;
    pillTimes = [];
}