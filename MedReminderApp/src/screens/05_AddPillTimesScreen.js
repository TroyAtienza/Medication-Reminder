import { StyleSheet, Text, View } from 'react-native';

const AddPillTimesScreen = (props) => {
  return (
    <View style={styles.container}>
        <Text>Pill Times!</Text>
    </View>
  );
}

const styles = StyleSheet.create({

});

export default AddPillTimesScreen;